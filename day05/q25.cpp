// Que - Given an array of size n and number k,find all elements that appear more than "n/k" 

#include<iostream>
using namespace std;

int main(){
    int N;
    cin>>N;
    int arr[N];
    // take array as input
    for(int i=0;i<N;i++){
        cin>>arr[i];
    }
    
    return 0;
}